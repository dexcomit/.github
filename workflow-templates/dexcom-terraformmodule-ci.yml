name: dexcom-terraformmodule-ci

on:
  push:
    branches: 
      - $default-branch
  pull_request:

jobs:
  terraformmodule:
    uses: dexcomit/github-workflows/.github/workflows/terraformmodule.yml@master
